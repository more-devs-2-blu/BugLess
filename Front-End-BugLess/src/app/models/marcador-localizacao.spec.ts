import { MarcadorLocalizacao } from './marcador-localizacao';

describe('MarcadorLocalizacao', () => {
  it('should create an instance', () => {
    expect(new MarcadorLocalizacao()).toBeTruthy();
  });
});
